cmake_minimum_required(VERSION 3.16...3.27)
project(brain)

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic -Wno-unknown-pragmas)
endif()

# find dependencies
find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(std_msgs REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(behaviortree_cpp REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(rerun_sdk REQUIRED)
find_package(Eigen3 REQUIRED)
find_package(booster_interface REQUIRED)
find_package(game_controller_interface REQUIRED)
find_package(vision_interface REQUIRED)
find_package(booster_msgs REQUIRED)
find_package(rosidl_default_generators REQUIRED)
find_package(OpenCV REQUIRED)
find_package(backward_ros REQUIRED)
find_package(tf2 REQUIRED)
find_package(tf2_ros REQUIRED)
find_package(tf2_geometry_msgs REQUIRED)
find_package(yaml-cpp REQUIRED)


# uncomment the following section in order to fill in
# further dependencies manually.
# find_package(<dependency> REQUIRED)

# TODO: These msgs are generated by detection pkg
rosidl_generate_interfaces(${PROJECT_NAME}
  "msg/Kick.msg"
  DEPENDENCIES std_msgs # Add packages that above messages depend on
)
ament_export_dependencies(rosidl_default_runtime)

# source files
file(GLOB SOURCE_FILES src/*.cpp)
add_executable(brain_node ${SOURCE_FILES})

execute_process(
  COMMAND git describe --always --dirty
  WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
  OUTPUT_VARIABLE BRAIN_GIT_DESCRIBE
  OUTPUT_STRIP_TRAILING_WHITESPACE
  ERROR_QUIET)
if(NOT BRAIN_GIT_DESCRIBE)
  set(BRAIN_GIT_DESCRIBE "unknown")
endif()
target_compile_definitions(
  brain_node PRIVATE BRAIN_GIT_DESCRIBE="${BRAIN_GIT_DESCRIBE}")

# The release SDK installer copies its public headers into the compiler's
# system include directories (normally /usr/local/include).
include(CheckIncludeFileCXX)
check_include_file_cxx(
  "booster/robot/b1/b1_loco_api.hpp"
  BOOSTER_RELEASE_SDK_HEADERS_FOUND)
if(NOT BOOSTER_RELEASE_SDK_HEADERS_FOUND)
  message(FATAL_ERROR
    "Booster Robotics release SDK headers were not found. Install the release "
    "SDK system-wide with its install.sh script before building this workspace.")
endif()

# otherwise connot find msg in same pkg 
# Find the typesupport target
rosidl_get_typesupport_target(typesupport_target ${PROJECT_NAME} "rosidl_typesupport_cpp")
# Link the typesupport target to your project
target_link_libraries(brain_node ${typesupport_target})


set_property(TARGET brain_node PROPERTY CXX_STANDARD 17)

target_link_libraries(brain_node rerun_sdk ${OpenCV_LIBS} yaml-cpp) 
target_link_libraries(brain_node ${backward_ros_LIBRARIES})
include_directories(${geometry_msgs_INCLUDE_DIRS})
include_directories(${std_msgs_INCLUDE_DIRS})
include_directories(${sensor_msgs_INCLUDE_DIRS})
include_directories(${EIGEN3_INCLUDE_DIR})
include_directories(${OpenCV_INCLUDE_DIRS})
include_directories(${CMAKE_CURRENT_BINARY_DIR}/rosidl_generator_cpp)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wno-narrowing -Wno-unused-parameter -Wno-sign-compare -Wno-missing-field-initializers -Wno-unused-variable")

ament_target_dependencies(brain_node rclcpp rosidl_typesupport_cpp std_msgs sensor_msgs behaviortree_cpp booster_interface vision_interface game_controller_interface booster_msgs tf2 tf2_ros tf2_geometry_msgs)

target_include_directories(brain_node PUBLIC
  $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
  $<INSTALL_INTERFACE:include>)
target_compile_features(brain_node PUBLIC c_std_99 cxx_std_17) # Require C99 and C++17
install(TARGETS
  brain_node
  DESTINATION lib/${PROJECT_NAME})

install(DIRECTORY launch
  DESTINATION share/${PROJECT_NAME})

install(DIRECTORY config
  DESTINATION share/${PROJECT_NAME})

install(DIRECTORY behavior_trees
  DESTINATION share/${PROJECT_NAME})

install(DIRECTORY tools
  DESTINATION share/${PROJECT_NAME})

if(BUILD_TESTING)
  add_executable(assist_strategy_policy_test
    test/assist_strategy_policy_test.cpp)
  target_include_directories(assist_strategy_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(assist_strategy_policy_test PRIVATE cxx_std_17)
  add_test(NAME assist_strategy_policy_test
    COMMAND assist_strategy_policy_test)

  add_executable(fall_recovery_policy_test
    test/fall_recovery_policy_test.cpp)
  target_include_directories(fall_recovery_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(fall_recovery_policy_test PRIVATE cxx_std_17)
  add_test(NAME fall_recovery_policy_test
    COMMAND fall_recovery_policy_test)

  add_executable(fallen_robot_avoidance_policy_test
    test/fallen_robot_avoidance_policy_test.cpp)
  target_include_directories(fallen_robot_avoidance_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(fallen_robot_avoidance_policy_test PRIVATE cxx_std_17)
  add_test(NAME fallen_robot_avoidance_policy_test
    COMMAND fallen_robot_avoidance_policy_test)

  add_executable(ball_search_policy_test
    test/ball_search_policy_test.cpp)
  target_include_directories(ball_search_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(ball_search_policy_test PRIVATE cxx_std_17)
  add_test(NAME ball_search_policy_test
    COMMAND ball_search_policy_test)

  add_executable(goalkeeper_ball_prediction_policy_test
    test/goalkeeper_ball_prediction_policy_test.cpp)
  target_include_directories(goalkeeper_ball_prediction_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(goalkeeper_ball_prediction_policy_test PRIVATE cxx_std_17)
  add_test(NAME goalkeeper_ball_prediction_policy_test
    COMMAND goalkeeper_ball_prediction_policy_test)

  find_package(Python3 COMPONENTS Interpreter REQUIRED)
  add_test(NAME goalkeeper_web_test
    COMMAND ${Python3_EXECUTABLE}
      ${CMAKE_CURRENT_SOURCE_DIR}/test/goalkeeper_web_test.py
      --server-dir ${CMAKE_CURRENT_SOURCE_DIR}/tools/goalkeeper_web)

  add_executable(head_scan_policy_test
    test/head_scan_policy_test.cpp)
  target_include_directories(head_scan_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(head_scan_policy_test PRIVATE cxx_std_17)
  add_test(NAME head_scan_policy_test
    COMMAND head_scan_policy_test)

  add_executable(robot_obstacle_policy_test
    test/robot_obstacle_policy_test.cpp)
  target_include_directories(robot_obstacle_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(robot_obstacle_policy_test PRIVATE cxx_std_17)
  add_test(NAME robot_obstacle_policy_test
    COMMAND robot_obstacle_policy_test)

  add_executable(robot_path_planner_policy_test
    test/robot_path_planner_policy_test.cpp)
  target_include_directories(robot_path_planner_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(robot_path_planner_policy_test PRIVATE cxx_std_17)
  add_test(NAME robot_path_planner_policy_test
    COMMAND robot_path_planner_policy_test)

  add_executable(obstacle_avoidance_logger_test
    test/obstacle_avoidance_logger_test.cpp
    src/obstacle_avoidance_logger.cpp)
  target_include_directories(obstacle_avoidance_logger_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(obstacle_avoidance_logger_test PRIVATE cxx_std_17)
  add_test(NAME obstacle_avoidance_logger_test
    COMMAND obstacle_avoidance_logger_test)

  add_executable(freekick_policy_test
    test/freekick_policy_test.cpp)
  target_include_directories(freekick_policy_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include)
  target_compile_features(freekick_policy_test PRIVATE cxx_std_17)
  add_test(NAME freekick_policy_test
    COMMAND freekick_policy_test)

  add_executable(booster_sdk_compatibility_test
    test/booster_sdk_compatibility_test.cpp)
  target_compile_features(booster_sdk_compatibility_test PRIVATE cxx_std_17)
  add_test(NAME booster_sdk_compatibility_test
    COMMAND booster_sdk_compatibility_test)

  find_package(ament_lint_auto REQUIRED)
  # the following line skips the linter which checks for copyrights
  # comment the line when a copyright and license is added to all source files
  set(ament_cmake_copyright_FOUND TRUE)
  # the following line skips cpplint (only works in a git repo)
  # comment the line when this package is in a git repo and when
  # a copyright and license is added to all source files
  set(ament_cmake_cpplint_FOUND TRUE)
  ament_lint_auto_find_test_dependencies()
endif()

ament_package()
